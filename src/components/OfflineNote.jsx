import React from "react";

const OfflineNote = () => (
  <div className="offline-note">
    <h3>Offline Mode Features</h3>
    <p>Common phrases, favorites, and history are available offline.</p>
  </div>
);

export default OfflineNote;